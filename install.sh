#!/bin/bash

# We need to ensure that brew or aptitude is intalled

# We need to ensure that tmux and neovim is installed

# Install nvm and latest version of nodejs and iojs

# Install virtualenvwrapper

# Setup configs neovim, taskwarrior, tmux
